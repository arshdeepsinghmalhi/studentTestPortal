# Google Sheets (userDetails sheet) – required for email lookup and attendance verification
GOOGLE_SHEETS_ID=1AInMgyVaj9XGexA8-z-JDFcHjPpsxs3iCZ6a0IrVVlM
GOOGLE_SHEETS_RANGE=userDetails!A:Z

# Service account: private key and client email (from Google Cloud Console → IAM → Service account key)
# Use literal \n for newlines in the key, inside double quotes.
GOOGLE_SERVICE_ACCOUNT_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Optional: use JSON file instead of env vars (set only if not using GOOGLE_SERVICE_ACCOUNT_* above)
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Optional: backend port (default 5000)
# PORT=5000
